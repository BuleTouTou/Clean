# MinIO / S3-compatible object storage (local development placeholder)
MINIO_ENABLED=false
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_BUCKET=clean
MINIO_SECURE=false
# Address used when constructing OSS URLs; keep local for development.
MINIO_PUBLIC_ENDPOINT=http://localhost:9000
