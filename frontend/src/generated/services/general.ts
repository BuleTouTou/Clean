/* eslint-disable */
/* biome-ignore-all */
/* stylelint-disable */
/* editorconfig-checker-disable-file */
/* oxlint-disable */
/* oxcformat-ignore */
/**
 * 房源数据清洗工具 - version 0.1.0
 *
 * 本地运行的北京房源数据清洗服务
 *
 * OpenAPI version: 3.1.0
 *
 *
 * NOTE: This file is auto generated by the alova's vscode plugin.
 *
 * https://worma.js.org
 *
 * **Do not edit the file manually.**
 */
import { alovaInstance } from '../index';
import { buildPayload, resolveTagDefaultConfig } from '../helper';
import * as defaultConfig from '.';
import type { Alova2Method, Alova2MethodConfig } from '../typed';
import type * as ComponentTypes from '../components';

const generalDefaultConfig = resolveTagDefaultConfig(defaultConfig, 'generalDefaultConfig');

export type ExtractResponded<
  UserDefinedConfig extends Alova2MethodConfig<any>,
  UnifiedConfigKey extends string,
  Default
> = UserDefinedConfig extends { transform: (...args: any[]) => any }
  ? Awaited<ReturnType<UserDefinedConfig['transform']>>
  : UnifiedConfigKey extends keyof typeof generalDefaultConfig
    ? (typeof generalDefaultConfig)[UnifiedConfigKey] extends { transform: (...args: any[]) => any }
      ? Awaited<ReturnType<(typeof generalDefaultConfig)[UnifiedConfigKey]['transform']>>
      : Default
    : Default;

interface loginExtraConfig {
  data: ComponentTypes.LoginRequest;
}
/**
 * ---
 *
 * [POST] Login
 *
 * **path:** /api/auth/login
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Username
 *   username: string
 *   // [title] Password
 *   password: string
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function login<Config extends Alova2MethodConfig<Record<string, any>> & loginExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'login', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/auth/login', generalDefaultConfig, login.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'login', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface getCurrentUserExtraConfig {}
/**
 * ---
 *
 * [GET] Me
 *
 * **path:** /api/auth/me
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function getCurrentUser<Config extends Alova2MethodConfig<Record<string, any>> & getCurrentUserExtraConfig>(
  config?: Config
): Alova2Method<ExtractResponded<Config, 'getCurrentUser', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/auth/me', generalDefaultConfig, getCurrentUser.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'getCurrentUser', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'GET'
  });
}

interface changeCurrentPasswordExtraConfig {
  data: ComponentTypes.ChangePasswordRequest;
}
/**
 * ---
 *
 * [POST] Change Current Password
 *
 * **path:** /api/auth/change-password
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Currentpassword
 *   currentPassword: string
 *   // [title] Newpassword
 *   newPassword: string
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function changeCurrentPassword<
  Config extends Alova2MethodConfig<Record<string, any>> & changeCurrentPasswordExtraConfig
>(config: Config): Alova2Method<ExtractResponded<Config, 'changeCurrentPassword', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload(
    '/api/auth/change-password',
    generalDefaultConfig,
    changeCurrentPassword.name,
    config
  );
  return alovaInstance.Request<ExtractResponded<Config, 'changeCurrentPassword', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface logoutExtraConfig {}
/**
 * ---
 *
 * [POST] Logout
 *
 * **path:** /api/auth/logout
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, boolean>
 * ```
 *
 */
export function logout<Config extends Alova2MethodConfig<Record<string, boolean>> & logoutExtraConfig>(
  config?: Config
): Alova2Method<ExtractResponded<Config, 'logout', Record<string, boolean>>> {
  const { url, data, mergedConfig } = buildPayload('/api/auth/logout', generalDefaultConfig, logout.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'logout', Record<string, boolean>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface listUsersExtraConfig {}
/**
 * ---
 *
 * [GET] Users
 *
 * **path:** /api/users
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function listUsers<Config extends Alova2MethodConfig<Record<string, any>> & listUsersExtraConfig>(
  config?: Config
): Alova2Method<ExtractResponded<Config, 'listUsers', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/users', generalDefaultConfig, listUsers.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'listUsers', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'GET'
  });
}

interface createUserExtraConfig {
  data: ComponentTypes.CreateUserRequest;
}
/**
 * ---
 *
 * [POST] Add User
 *
 * **path:** /api/users
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Username
 *   username: string
 *   // [title] Name
 *   name: string
 *   // [title] Initialpassword
 *   initialPassword: string
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function createUser<Config extends Alova2MethodConfig<Record<string, any>> & createUserExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'createUser', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/users', generalDefaultConfig, createUser.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'createUser', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface resetUserPasswordExtraConfig {
  pathParams: {
    /**
     * User Id
     * ---
     */

    user_id: number;
  };
  data: ComponentTypes.ResetPasswordRequest;
}
/**
 * ---
 *
 * [POST] Reset Password
 *
 * **path:** /api/users/{user_id}/reset-password
 *
 * ---
 *
 * **Path Parameters**
 * ```ts
 * type PathParameters = {
 *   // [title] User Id
 *   user_id: number
 * }
 * ```
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Initialpassword
 *   initialPassword: string
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function resetUserPassword<
  Config extends Alova2MethodConfig<Record<string, any>> & resetUserPasswordExtraConfig
>(config: Config): Alova2Method<ExtractResponded<Config, 'resetUserPassword', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload(
    '/api/users/{user_id}/reset-password',
    generalDefaultConfig,
    resetUserPassword.name,
    config
  );
  return alovaInstance.Request<ExtractResponded<Config, 'resetUserPassword', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface getStatusExtraConfig {}
/**
 * ---
 *
 * [GET] Status
 *
 * **path:** /api/status
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function getStatus<Config extends Alova2MethodConfig<Record<string, any>> & getStatusExtraConfig>(
  config?: Config
): Alova2Method<ExtractResponded<Config, 'getStatus', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/status', generalDefaultConfig, getStatus.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'getStatus', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'GET'
  });
}

interface listReportsExtraConfig {
  params: {
    /**
     * Page
     * ---
     */

    page?: number;
    /**
     * Pagesize
     * ---
     */

    pageSize?: number;
    /**
     * Kind
     * ---
     */

    kind?: 'sale' | 'rent' | null;
    /**
     * Ownersearch
     * ---
     */

    ownerSearch?: string | null;
  };
}
/**
 * ---
 *
 * [GET] Reports
 *
 * **path:** /api/reports
 *
 * ---
 *
 * **Query Parameters**
 * ```ts
 * type QueryParameters = {
 *   // [title] Page
 *   page?: number
 *   // [title] Pagesize
 *   pageSize?: number
 *   // [title] Kind
 *   kind?: "sale" | "rent" | null
 *   // [title] Ownersearch
 *   ownerSearch?: string | null
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function listReports<Config extends Alova2MethodConfig<Record<string, any>> & listReportsExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'listReports', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/reports', generalDefaultConfig, listReports.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'listReports', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'GET'
  });
}

interface getReportExtraConfig {
  pathParams: {
    /**
     * Report Id
     * ---
     */

    report_id: number;
  };
}
/**
 * ---
 *
 * [GET] Report Detail
 *
 * **path:** /api/reports/{report_id}
 *
 * ---
 *
 * **Path Parameters**
 * ```ts
 * type PathParameters = {
 *   // [title] Report Id
 *   report_id: number
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function getReport<Config extends Alova2MethodConfig<Record<string, any>> & getReportExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'getReport', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload(
    '/api/reports/{report_id}',
    generalDefaultConfig,
    getReport.name,
    config
  );
  return alovaInstance.Request<ExtractResponded<Config, 'getReport', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'GET'
  });
}

interface downloadReportExtraConfig {
  pathParams: {
    /**
     * Report Id
     * ---
     */

    report_id: number;
  };
}
/**
 * ---
 *
 * [GET] Download Report
 *
 * **path:** /api/reports/{report_id}/download
 *
 * ---
 *
 * **Path Parameters**
 * ```ts
 * type PathParameters = {
 *   // [title] Report Id
 *   report_id: number
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = null
 * ```
 *
 */
export function downloadReport<Config extends Alova2MethodConfig<null> & downloadReportExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'downloadReport', null>> {
  const { url, data, mergedConfig } = buildPayload(
    '/api/reports/{report_id}/download',
    generalDefaultConfig,
    downloadReport.name,
    config
  );
  return alovaInstance.Request<ExtractResponded<Config, 'downloadReport', null>>({
    ...mergedConfig,
    url,
    data,
    method: 'GET'
  });
}

interface createTaskExtraConfig {
  data: ComponentTypes.TaskRequest;
}
/**
 * ---
 *
 * [POST] Create Task
 *
 * **path:** /api/task
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Kind
 *   kind: "sale" | "rent"
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function createTask<Config extends Alova2MethodConfig<Record<string, any>> & createTaskExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'createTask', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/task', generalDefaultConfig, createTask.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'createTask', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface uploadSourceFileExtraConfig {}
/**
 * ---
 *
 * [POST] Upload
 *
 * **path:** /api/upload
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function uploadSourceFile<Config extends Alova2MethodConfig<Record<string, any>> & uploadSourceFileExtraConfig>(
  config?: Config
): Alova2Method<ExtractResponded<Config, 'uploadSourceFile', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/upload', generalDefaultConfig, uploadSourceFile.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'uploadSourceFile', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface selectSheetExtraConfig {
  data: ComponentTypes.SheetRequest;
}
/**
 * ---
 *
 * [POST] Select Sheet
 *
 * **path:** /api/select-sheet
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Taskid
 *   taskId: string
 *   // [title] Sheet
 *   sheet: string
 *   // [title] Headerrow
 *   headerRow: number
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function selectSheet<Config extends Alova2MethodConfig<Record<string, any>> & selectSheetExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'selectSheet', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/select-sheet', generalDefaultConfig, selectSheet.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'selectSheet', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface saveMappingExtraConfig {
  data: ComponentTypes.MappingRequest;
}
/**
 * ---
 *
 * [POST] Mapping
 *
 * **path:** /api/mapping
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Taskid
 *   taskId: string
 *   // [title] Mapping
 *   mapping: Record<string, string | string[]>
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function saveMapping<Config extends Alova2MethodConfig<Record<string, any>> & saveMappingExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'saveMapping', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/mapping', generalDefaultConfig, saveMapping.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'saveMapping', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface reviewEstateExtraConfig {
  data: ComponentTypes.ReviewRequest;
}
/**
 * ---
 *
 * [POST] Review
 *
 * **path:** /api/review
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Taskid
 *   taskId: string
 *   // [title] Selected
 *   selected?: Record<string, string>
 *   // [title] Entrustdate
 *   entrustDate?: string | null
 *   // [title] Persistrules
 *   persistRules?: boolean
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, boolean>
 * ```
 *
 */
export function reviewEstate<Config extends Alova2MethodConfig<Record<string, boolean>> & reviewEstateExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'reviewEstate', Record<string, boolean>>> {
  const { url, data, mergedConfig } = buildPayload('/api/review', generalDefaultConfig, reviewEstate.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'reviewEstate', Record<string, boolean>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface reviewBuildingExtraConfig {
  data: ComponentTypes.ReviewRequest;
}
/**
 * ---
 *
 * [POST] Building Review
 *
 * **path:** /api/building-review
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Taskid
 *   taskId: string
 *   // [title] Selected
 *   selected?: Record<string, string>
 *   // [title] Entrustdate
 *   entrustDate?: string | null
 *   // [title] Persistrules
 *   persistRules?: boolean
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function reviewBuilding<Config extends Alova2MethodConfig<Record<string, any>> & reviewBuildingExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'reviewBuilding', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload(
    '/api/building-review',
    generalDefaultConfig,
    reviewBuilding.name,
    config
  );
  return alovaInstance.Request<ExtractResponded<Config, 'reviewBuilding', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface confirmBuildingExtraConfig {
  data: ComponentTypes.ReviewRequest;
}
/**
 * ---
 *
 * [POST] Building Confirm
 *
 * **path:** /api/building-confirm
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Taskid
 *   taskId: string
 *   // [title] Selected
 *   selected?: Record<string, string>
 *   // [title] Entrustdate
 *   entrustDate?: string | null
 *   // [title] Persistrules
 *   persistRules?: boolean
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, boolean>
 * ```
 *
 */
export function confirmBuilding<
  Config extends Alova2MethodConfig<Record<string, boolean>> & confirmBuildingExtraConfig
>(config: Config): Alova2Method<ExtractResponded<Config, 'confirmBuilding', Record<string, boolean>>> {
  const { url, data, mergedConfig } = buildPayload(
    '/api/building-confirm',
    generalDefaultConfig,
    confirmBuilding.name,
    config
  );
  return alovaInstance.Request<ExtractResponded<Config, 'confirmBuilding', Record<string, boolean>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface exportTaskExtraConfig {
  data: ComponentTypes.ExportRequest;
}
/**
 * ---
 *
 * [POST] Export
 *
 * **path:** /api/export
 *
 * ---
 *
 * **RequestBody**
 * ```ts
 * type RequestBody = {
 *   // [title] Taskid
 *   taskId: string
 *   // [title] Cleanonly
 *   cleanOnly?: boolean
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = Record<string, any>
 * ```
 *
 */
export function exportTask<Config extends Alova2MethodConfig<Record<string, any>> & exportTaskExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'exportTask', Record<string, any>>> {
  const { url, data, mergedConfig } = buildPayload('/api/export', generalDefaultConfig, exportTask.name, config);
  return alovaInstance.Request<ExtractResponded<Config, 'exportTask', Record<string, any>>>({
    ...mergedConfig,
    url,
    data,
    method: 'POST'
  });
}

interface downloadFileExtraConfig {
  pathParams: {
    /**
     * Token
     * ---
     */

    token: string;
  };
}
/**
 * ---
 *
 * [GET] Download
 *
 * **path:** /download/{token}
 *
 * ---
 *
 * **Path Parameters**
 * ```ts
 * type PathParameters = {
 *   // [title] Token
 *   token: string
 * }
 * ```
 *
 * ---
 *
 * **Response**
 * ```ts
 * type Response = null
 * ```
 *
 */
export function downloadFile<Config extends Alova2MethodConfig<null> & downloadFileExtraConfig>(
  config: Config
): Alova2Method<ExtractResponded<Config, 'downloadFile', null>> {
  const { url, data, mergedConfig } = buildPayload(
    '/download/{token}',
    generalDefaultConfig,
    downloadFile.name,
    config
  );
  return alovaInstance.Request<ExtractResponded<Config, 'downloadFile', null>>({
    ...mergedConfig,
    url,
    data,
    method: 'GET'
  });
}
